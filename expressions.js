//Expression to extract Movie Titles
const movieRegex = /^Title \((?:\d{4})\)$/;

//Expression to extract Song lyrics;

//Expression to extract the date
const dateRegex = /^(\d{2})-(\w{3})-(\d{4})$/;

//Expression to extract ISBN numbers
const isbnRegex = /^ISBN (\d{3})-(\d)-(\d{3})-(\d{5})-(\d)$/;
